import org.junit.jupiter.api.Test;

class WikiClientTest {
    @Test
    public void teest() throws Exception {
//        PageInfo resp = new WikiClient().findLinks("java");
//        assertNotNull(resp);
//        assertEquals("Java", resp.getTitle());
//        assertTrue(resp.getLinks().size() > 0);

    }
}